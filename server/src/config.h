#pragma once

#include <string>
#include <vector>
#include <algorithm>

using namespace std::string_literals;

namespace BuildleGame {
    const size_t LetterCount = 5;
    const size_t MaxAttempts = 2;
    extern const std::vector<std::string> Builds;
};